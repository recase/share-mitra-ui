import { CostTransformPipe } from './cost-transform.pipe';

describe('CostTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CostTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
