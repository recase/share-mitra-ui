import { UnitTransformPipe } from './unit-transform.pipe';

describe('UnitTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
